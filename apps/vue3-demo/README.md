# Vue3 + TypeScript + Vite + Pinia Demo

这是一个使用现代 Vue3 技术栈构建的演示项目。

## 技术栈

- **Vue 3** - 渐进式 JavaScript 框架
- **TypeScript** - 类型安全的 JavaScript 超集
- **Vite** - 极速的开发服务器和构建工具
- **Pinia** - Vue 的现代状态管理库
- **Vue Router** - Vue.js 官方路由管理器

## 项目结构

```
src/
├── components/          # 可复用组件
│   └── HelloWorld.vue
├── views/              # 页面组件
│   ├── HomeView.vue
│   ├── CounterView.vue
│   └── TodosView.vue
├── stores/             # Pinia 状态管理
│   ├── counter.ts
│   └── todos.ts
├── router/             # 路由配置
│   └── index.ts
├── types/              # TypeScript 类型定义
│   └── index.ts
├── utils/              # 工具函数
│   └── index.ts
├── App.vue             # 根组件
└── main.ts             # 应用入口
```

## 功能特性

- 🏠 **首页** - 项目介绍和导航
- 🔢 **计数器** - Pinia 状态管理示例
- ✅ **待办事项** - 完整的 CRUD 操作示例
- 📱 **响应式设计** - 适配各种屏幕尺寸
- 🎨 **现代化 UI** - 美观的渐变和动画效果

## 开发命令

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev

# 构建生产版本
pnpm build

# 预览构建结果
pnpm preview

# 类型检查
pnpm type-check
```

## 开发服务器

项目将在 `http://localhost:3000` 启动，支持热重载。

## 构建

构建后的文件将输出到 `dist` 目录，可以部署到任何静态文件服务器。
